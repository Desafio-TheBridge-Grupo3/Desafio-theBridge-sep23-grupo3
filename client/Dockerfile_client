# Utilizar la imagen base de Node.js 20.9 hasta que ciber tenga imagen segura
FROM node:20.9-alpine

WORKDIR /app

COPY . ./client

RUN cd client && npm install

EXPOSE 5173

CMD ["sh", "-c", "cd client && npm run dev -- --port 5173 --host 0.0.0.0"]
